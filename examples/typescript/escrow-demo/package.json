{
  "name": "escrow-demo",
  "version": "1.0.0",
  "description": "Executable demo showing full escrow workflow using @solana/kit",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "start-validator": "solana-test-validator -r -q --bpf-program Escrowae7RaUfNn4oEZHywMXE5zWzYCXenwrCDaEoifg ../../../target/deploy/escrow_program.so >/tmp/escrow-validator.log 2>&1 &",
    "kill-validator": "pgrep -f solana-test-validator >/dev/null && pkill -f solana-test-validator || true",
    "wait-validator": "i=0; until solana --url http://127.0.0.1:8899 cluster-version >/dev/null 2>&1; do i=$((i+1)); [ $i -ge 30 ] && echo \"validator not ready\" && exit 1; sleep 1; done",
    "start-with-validator": "pnpm kill-validator && pnpm start-validator && pnpm wait-validator && pnpm start && pnpm kill-validator",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solana-program/compute-budget": "^0.16.0",
    "@solana-program/system": "^0.12.0",
    "@solana-program/token": "^0.14.0",
    "@solana/escrow": "workspace:*",
    "@solana/kit": "^6.10.0",
    "@solana/kit-plugins": "^0.12.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.0",
    "typescript": "^5.9.3"
  }
}
